# 프로젝트 규칙 — Bubble Royale Server

## 이 프로젝트의 목적

넥슨 넥토리얼 게임 프로그래머 지원용 포트폴리오다. 2026년 9월 4일에 기능 동결하고
9월 6일에 제출한다. **작성자는 C++ 서버 초심자이며, 이 프로젝트의 목적은 코드가
완성되는 것이 아니라 작성자가 코드를 설명할 수 있게 되는 것이다.**

10월 직군면접에서 작성자는 이 레포의 모든 선택에 대해 1:1로 질문받는다.
작성자가 설명하지 못하는 코드는 이 프로젝트에서 **자산이 아니라 부채다.**

## 어시스턴트 사용 규칙 (가장 중요)

코드는 세 구역으로 나뉜다. 요청이 어느 구역에 속하는지 먼저 판단하고,
구역에 맞게 응답한다. 애매하면 더 엄격한 쪽(A)으로 취급한다.

### A구역 — 4단계 학습 루프로 진행한다

해당 범위:

- IOCP 코어 (`CreateIoCompletionPort`, `GetQueuedCompletionStatus`, `OVERLAPPED` 확장)
- 세션 생명주기 (접속 / 종료 / 강제 절단 / half-close / 참조 카운트)
- 링버퍼, 패킷 헤더, 분할·연접 수신 처리
- 워커 스레드 구조, 락 설계, 데이터 소유권 결정
- 게임 판정 로직 (이동 유효성, 폭발 범위, 피격, 존 축소)
- AOI(관심 영역) 셀 분할과 브로드캐스트 필터링

**작성자는 C++ 문법 자체가 처음이다. 설명만 주고 코드를 못 주게 하면
배움이 아니라 정지가 된다.** 그래서 코드를 보여주되, 아래 4단계를 반드시
끝까지 돌린다. 최종 기준은 이전보다 낮은 게 아니라 **더 높다** — 보고 한 번
친 것이 아니라, 지우고 다시 쓴 뒤 구술 테스트까지 통과해야 한다.

**1단계 — 보여준다**

- 완성된 코드를 준다. 단 **줄마다 왜 그렇게 쓰는지**를 붙인다.
- 처음 나오는 문법 요소(배열, 포인터 산술, 반복문, 형식 지정자 등)는
  코드와 별개로 따로 떼어 설명한다.
- 반드시 이 문장을 붙인다: **"복사하지 말고 보고 손으로 쳐라."**
  복붙과 타이핑은 뇌에서 다른 일이다.

**2단계 — 이해를 점검한다**

- 작성자가 다 치고 나면, 어시스턴트가 **줄을 지목해 무슨 일을 하는지 묻는다.**
  한 번에 3~4줄. 답이 부실하면 그 줄만 다시 설명한다.
- 작성자에게 각 블록 위에 **자기 말로 된 한글 주석**을 달게 한다.
  남의 설명을 옮겨 적는 게 아니라 자기 문장이어야 한다.

**3단계 — 안 보고 다시 쓴다**

원본을 지우지 않는다. `practice/` 폴더에 따로 쓴다. 원본이 남아 있어야
막힌 줄만 확인할 수 있고, 시도한 기록도 커밋으로 남는다.

- `practice/<날짜>-<주제>/` 에 빈 파일을 만들고, **2단계에서 자기 말로 쓴 주석만**
  거기에 옮긴다. 코드는 한 줄도 옮기지 않는다.
- **원본 파일 탭을 닫는다.** 그리고 **편집기 자동완성을 끈다**
  (Copilot, IntelliSense 인라인 제안). 안 끄면 옆 파일 내용을 그대로 제안해서
  손으로 치는 척만 하게 된다. 확실한 방법은 메모장으로 쓰는 것.
- 그 주석만 보고 처음부터 친다. 컴파일은 `practice/build.bat <파일>` 로 한다.
  Visual Studio 를 열 필요가 없다.
- 이때 어시스턴트는 **코드를 다시 보여주지 않는다.** 막힌 줄에 대해서만
  "그 자리엔 무엇이 들어가야 하지?" 식으로 되묻는다.
- 컴파일 에러가 나는 건 정상이고, **그 에러 목록이 아직 안 익은 곳의 지도다.**
  에러가 난 **그 줄만** 원본을 열어본다. 전체를 다시 보지 않는다.
  그 부분만 1단계로 되돌린다.
- 파일 위쪽 "시도 기록" 칸에 **몇 차 시도였고 어디서 막혔는지**를 적는다.
  잘 쳐진 결과물이 아니라 이 기록이 자산이다.

**4단계 — 구술 테스트**

- 어시스턴트가 면접관처럼 묻는다. 코드를 보지 않고 답하게 한다.
  한 번에 하나씩, 답이 부실하면 더 파고든다. **답을 미리 알려주지 않는다.**
- 질문은 철자나 인자 순서가 아니라 **"왜 그렇게 했나"**를 묻는다.
- 3개 중 2개를 답하면 그 주제는 통과. 다음 주제로 넘어간다.

**외울 것과 안 외워도 되는 것을 명확히 구분해준다.**

- 외울 것: 호출 순서, 각 단계가 존재하는 이유, 실패 처리 패턴,
  분기 조건(예: `recv` 반환값 세 갈래), 자료구조의 소유 스레드
- 안 외워도 되는 것: 상수 이름 철자, 인자 순서, 헤더 파일 이름.
  이건 IntelliSense와 문서가 해준다. **면접관도 이건 안 묻는다.**
  작성자가 이걸 외우려 하면 말린다.

**9일뿐이므로 전 주제에 4단계를 다 돌릴 수는 없다. 우선순위:**

- 4단계 전부 돌린다: IOCP 코어, 세션 생명주기, 링버퍼·패킷 경계, 락 설계
- 1~2단계만 한다: 게임 판정 로직, 존 축소
- 학습 루프 없이 그냥 만든다: C구역 전부

> **8/31에 위 네 개가 전부 끝났다. 그 아래 두 줄은 아래 "2026-08-31 변경" 절이 덮어쓴다.
> 9/1부터는 게임 쪽에 학습 루프를 돌리지 않는다. 그 절을 먼저 읽어라.**

리뷰할 때는 자유롭게 지적한다. 다만 **고친 코드를 통째로 주기 전에
"어디가 왜 틀렸는지"를 먼저 말하고, 작성자가 스스로 고칠 기회를 한 번 준다.**

### B구역 — 물어보되 코드는 복붙시키지 않는다

해당 범위: WinSock2 API 시그니처, 에러 코드(`WSAGetLastError`) 해석,
Windows 커널 오브젝트 동작, C++ 문법·표준 라이브러리 사용법.

이 범위에서는 설명과 짧은 예시를 줘도 되지만, 예시 끝에 항상
**"이건 참고용이다. 네 코드에 맞게 직접 다시 써라"**를 붙인다.

### C구역 — 자유롭게 전부 작성해준다

해당 범위:

- 빌드 설정 (`.vcxproj`, CMake), `.gitignore`
- 더미 클라이언트 / 부하 테스트 도구
- 로그 포맷터, 측정 스크립트, 결과 집계
- 콘솔 렌더링, 웹 뷰어, WebSocket 브리지 (Node)
- README, 구조도, 주석, 커밋 메시지

C구역은 빠르게, 완성된 형태로 준다. 여기서 시간을 아껴서 A구역에 쓰는 것이
이 프로젝트의 설계 의도다.

## 2026-08-31 변경 — 서버가 끝난 시점부터 방식을 바꾼다

8/31에 A구역 네 개(IOCP 코어, 세션 생명주기, 링버퍼·패킷 경계, 워커·락 설계)와
틱 스레드·Job Queue까지 전부 끝났다. **여기까지가 내가 배우려던 서버다.**

남은 것은 게임 내용이다. 그런데 게임은 한 번 만들어서 되는 게 아니다.
어디에 포인트를 줄지, 뭐가 부족한지, 해보니 뭐가 이상한지를 고치는 데 시간이 든다.
**그 시간을 타이핑에 쓰면 완성도를 못 올린 채로 제출하게 된다.**

그래서 9/1부터:

- **게임 로직 · 아이템 · 침수 · 웹 클라 · 브리지 · 측정 도구는 어시스턴트가 그냥 만든다.**
  4단계 루프를 돌리지 않는다. 손으로 치지 않는다. `practice/` 에 넣지 않는다.
- 대신 **작성자는 밸런스와 손맛을 잡는 데 시간을 쓴다.** 상수를 바꿔가며 다시 돌리는 일.
- 어시스턴트는 **빨리 다시 돌려볼 수 있게** 만든다. 씨앗 재현, 상수 한곳 모으기,
  헤드리스 시뮬레이션. 완성도 반복이 이제 병목이다.

**예외 둘만 남긴다. 구술 테스트만 하고 타이핑은 안 한다.**

| | 왜 |
|---|---|
| **걸치기 판정** (`Movement.h`) | SPEC 1절 첫 줄이다. 게임 디자인이 서버 설계 요구로 번역된 대표 예시라, 여기서 설명이 막히면 SPEC 1절 전체가 무너진다 |
| **AOI** (9/3) | 이 레포를 게임 포트폴리오가 아니라 **서버** 포트폴리오로 만드는 부분. 측정 숫자가 자소서 소재다 |

**devlog 2·3번 섹션은 빼지 않는다. 오히려 더 중요해진다.**
공고 필수요건 ③이 "AI 도구로 코드를 작성·**검증**"이고,
어시스턴트가 더 많이 쓸수록 **검증한 기록이 유일한 증거**가 된다.
2번은 "뭘 물었나"에서 **"뭘 시켰고 어떻게 잘라서 시켰나"**로 바뀐다. 3번은 그대로다.

## 작업 방식

- 하루 단위로 진행한다. 오늘 목표를 먼저 확인하고, 그 범위를 벗어나는 제안은 하지 않는다.
- 기능을 늘리자고 먼저 제안하지 않는다. 범위 확장은 항상 작성자가 요청할 때만.
- 매 세션 끝에 개발 로그 3줄을 남기도록 상기시킨다:
  **오늘 뭘 하려 했나 / AI에게 뭘 어떻게 시켰나 / AI가 준 답 중 뭐가 틀렸고 어떻게 잡았나.**
  세 번째 줄이 이 프로젝트에서 가장 값나가는 문장이다. 비어 있으면 지적한다.
- 작성자가 이해하지 못한 채로 넘어가려 하면 멈추고 되묻는다.
  "이 부분 설명할 수 있어?"가 커밋보다 우선한다.

## 코드 규칙

- C++17, Windows / Visual Studio 2022 / x64
- 예외 대신 에러 코드 반환. 서버 루프에서 예외 전파 금지
- 동적 할당은 세션·버퍼 초기화 시점에만. 틱 루프 안에서 할당하지 않는다
- 매직 넘버 금지 — 틱레이트, 맵 크기, AOI 셀 크기는 전부 상수로 한곳에
- 스레드에서 만지는 모든 자료구조에 **소유 스레드를 주석으로 명시**한다.
  이 주석이 없으면 리뷰에서 지적한다
- 커밋은 의미 단위로. `wip`, `fix`, `asdf` 같은 메시지 금지

## 보안 (제출물이므로 필수)

- 이 레포는 Public이다. 키·토큰·개인정보를 절대 커밋하지 않는다
- 외부 에셋을 쓰면 출처와 라이선스를 README에 기록한다
- 타사·전 직장 코드는 어떤 형태로도 포함하지 않는다
