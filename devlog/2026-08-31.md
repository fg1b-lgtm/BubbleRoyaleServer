# 2026-08-31 (D-6) — 틱 스레드와 주문 꽂이

**1. 오늘 하려던 것**

워커 넷이 각자 패킷을 처리하던 것을 틱 스레드 하나로 몰아주는 것.

에코일 때는 문제가 없었다. 게임판이 생기면 다르다.
물풍선이 터지면 블록과 캐릭터를 같이 건드리는데, 그걸 워커 넷이 동시에 하면
자물쇠가 몇 개 필요한지 아무도 못 답하게 된다.
그래서 게임 처리는 한 명한테 다 준다. 주인이 하나면 자물쇠가 없다.

워커는 패킷을 잘라서 꽂이에 꽂고 바로 다음 손님에게 간다.
틱 스레드가 30Hz로 꽂이를 통째로 가져가서 처리한다.

완료 조건은 패킷이 워커에서 틱 스레드로 넘어가 처리되는 것이었다.

```
접속 2560   entered 2560   left 2560   fully closed 2560
probe 10 PASS / 0 FAIL      봇 에러 0
job queue full 0            tick behind 0
```

12초에 패킷 7742개가 워커에서 틱 스레드로 건너갔고 틱은 한 번도 안 밀렸다.
네 숫자가 같다는 게 꽂이를 거쳐도 새는 세션이 없다는 뜻이다.

로그 순서도 확인했다. `closing` 다음에 `left`가 나오고 그다음이 `fully closed`다.
꽂이에 꽂힌 주문이 참조를 하나 들고 있어서 그동안 세션이 안 지워진 것이다.

어제 못 맞힌 구술도 다시 했다. 소유 스레드 문제다.
`g_sessions`에 락이 필요 없다고 답해서 틀렸는데, 장면을 하나 받고 나서 스스로 고쳤다.
어제는 세 번 밀어야 나왔고 오늘은 한 번 밀어서 나왔다.

오늘 것 구술은 2승 1패로 통과했다.
못 맞힌 건 `CloseSession`에서 Leave 주문을 꽂고 나서 `Release`를 해도 왜 안 터지느냐였다.
주문이 참조를 하나 들고 있어서 그 `Release`가 마지막이 될 수 없다는 게 답인데,
"진행 중인 주문이 다 끝나야 지워진다"까지만 말하고 거기서 멈췄다.
어제 3번 칸에 내가 쓴 문장이 그대로 답이었다. 놓는 것은 항상 맨 마지막이다.

> (여기는 내가 다시 쓴다)

**2. AI에게 물은 것**

> (여기는 내가 다시 쓴다)

**3. AI가 틀린 것 / 내가 어떻게 잡았나**

- 틱 루프 타이밍 주석이 틀렸다.

  AI가 `// 더하기만 하면 처리 시간이 조금씩 쌓여서 점점 느려진다` 라고 써줬다.
  직전 target에서 33을 더하면 처리 시간은 안 쌓인다. 저건 지금 시각에서 더할 때만 맞는 말이다.

  진짜 이유는 33이 정확한 값이 아니라는 것이다. 1000/30은 33.333이다.
  33씩 더하면 한 틱마다 0.333ms씩 빨라져서 1분이면 0.6초, 5분 한 판이면 3초가 어긋난다.
  `GameConstants.h`에 시간을 전부 틱으로 적어놨으니 그게 그대로 게임 시각이 된다.

  이건 내가 구술 4번을 못 맞혀서 드러났다. 답을 확인하다 AI가 자기 주석이 틀렸다고 했다.

  > (여기는 내가 다시 쓴다)

- AI가 `ProcessPackets`의 `if (s->closing == 1) return;` 을 지우라고 했다.
  안 지우고 뒀는데 그게 맞았다. 이제 틱 스레드도 `CloseSession`을 부를 수 있어서
  있는 쪽이 맞다. AI도 나중에 그대로 두라고 말을 바꿨다.

- AI가 어제와 똑같이 문자열 이스케이프를 또 깨먹었다.
  파일에 `%ld\n` 을 넣으려다 `\n` 이 진짜 줄바꿈으로 들어갔다.
  어제 같은 걸로 네 번 넘어졌는데 오늘 또 했다.

3단계(안 보고 다시 쓰기)는 `practice/2026-08-31-jobqueue/` 에 만들어뒀다.
이번 건 서버가 아니라 채점기가 붙어 있어서 실행하면 PASS/FAIL이 나온다.
화·수·금 저녁에 학교에서 맥북으로 할 거라 `win_compat.h` 와 `build.sh` 도 같이 만들었다.
소켓 들어가는 연습은 맥에서 안 된다. IOCP는 맥에 없는 물건이다.

**방식을 바꿨다**

여기서 서버가 끝났다. 남은 건 게임 내용인데, 게임은 한 번 만들어서 되는 게 아니다.
어디가 이상한지 고치는 데 시간이 드는데 그 시간을 타이핑에 쓰면
완성도를 못 올린 채로 제출하게 된다.

그래서 9/1부터 게임 쪽은 AI가 그냥 만든다. 학습 루프를 안 돌린다.
예외는 AOI 하나. 그건 게임이 아니라 서버라서 남긴다.
CLAUDE.md에 절을 하나 추가해서 적어놨다.

**그 뒤로 오늘 한 것**

9/1과 9/2 분량을 앞당겼다.

- 고정소수점 이동, 걸치기 판정, 코너 보정
- 45x39 맵 생성. 봄버맨식 기둥 + 블록 + 스폰
- 물풍선, 십자 폭발, 연쇄, 갇힘, 블록 파괴, 아이템
- 침수 3단계 + 최종 구역 안 수위 상승
- 스냅샷 패킷, Node WebSocket 다리, 웹 Canvas 클라이언트
- 판의 생명주기 (기다림 / 카운트다운 / 진행 / 결과), 관전, 라운드제
- 킬 드롭, 울트라, 최종 보급
- 검증 도구 다섯 개: `movetest` `bubbletest` `floodtest` `maptest` `roundsim`

```
movetest 25 · bubbletest 58 · floodtest 33 · maptest 7 · probe 10 · wstest 16
전부 0 FAIL · 봇 26개 에러 0 · 봇 20판 무승부 0
```

> (여기는 내가 다시 쓴다)

**AI가 틀린 것 — 오후 이후**

- **걸치기를 잘못 만들었다.** AI가 "판정 타일이 늦게 따라온다"로 만들었다.
  새 칸에 68% 들어가야 판정이 옮겨가는 방식이었다.

  돌려보니 물풍선이 발밑이 아니라 방금 지나온 칸에 깔렸다. 그걸 보고 내가 말했다.
  캐릭터 몸의 과반수가 있는 곳이 캐릭터가 있는 곳이다.

  맞았다. 몸이 중심 기준으로 대칭이니 그건 그냥 중심이 있는 칸이고 계산이 필요 없다.
  걸치기는 판정을 늦춰서 생기는 게 아니라 몸이 타일보다 작아서 생기는 것이다.
  고치니 물풍선 위치도 같이 고쳐졌다. 규칙 하나가 두 군데를 먹고 있었다.

- **갇힌 사람을 죽이는 방법도 틀렸다.** AI가 다른 물풍선으로 죽게 만들었다.
  크아는 몸으로 부딪쳐서 터뜨린다. 이것도 내가 말해서 고쳤다.

  고치고 나니 설계가 더 나아졌다. 죽이려면 거리를 좁혀야 하니 마무리 자체가 위험해지고,
  갇힌 쪽이 기어서 도망칠 수 있는 게 그제서야 의미가 생겼다.

  > (여기는 내가 다시 쓴다)

- 스폰 두 개가 3칸 거리였다. 조각 안에서는 잘 떨어뜨려 놨는데 조각 경계 너머를 안 봤다.
  물줄기 사거리가 2인데 3칸이면 시작하자마자 사정권이다.
  `roundsim`을 만들어 돌린 첫 판에서 나왔다.

- 물풍선을 놓은 게 화면에 안 나갔다. `GameTick`이 맨 앞에서 이벤트를 비우는데
  설치는 그 앞 단계에서 일어난다. 시험이 먼저 잡았다.

- 연쇄 간격이 2틱이었다 3틱이었다 했다. 배열에서 앞에 있냐 뒤에 있냐로 갈렸다.
  같은 상황인데 결과가 다르고 설명도 못 한다.

- 문자열 이스케이프를 오늘만 다섯 번 더 깨먹었다. 어제 네 번, 오늘 다섯 번이다.

- AI가 디버그 출력을 `printf("a=%d b=%d", 함수(), 상태)` 로 찍고
  멀쩡한 코드를 버그라고 했다. MSVC는 인자를 오른쪽부터 평가해서
  함수가 상태를 바꾸기 전 값이 찍힌 것이었다.

- 시험이 14개 깨진 줄 알고 한참 봤는데 그냥 빌드가 안 된 거였다.
  빌드 출력을 죽여놔서 실패한 줄 몰랐고 옛 exe가 그대로 돌았다.

**진도**

8/31 하루에 9/1을 통째로, 9/2를 대부분 당겼다. 남은 건 9/3 AOI와 측정.

> (여기는 내가 다시 쓴다)
