# Bubble Royale Server — 스펙 & 9일 빌드 계획

C++ / IOCP 기반 실시간 멀티플레이 게임 서버.
크레이지아케이드식 격자 대전에 **.io식 대규모 난입**과 **배틀로얄 존 축소**를 얹었다.

- 개발 기간: 2026.08.27 ~ 09.04 (9일)
- 환경: Windows 11 / Visual Studio 2022 / C++17 / x64 / WinSock2 + IOCP
- 레포: Public (첫 커밋 전 `.gitignore` 먼저)

---

## 1. 무엇을 만드는가

격자 맵 위에서 최대 30명이 동시에 뛰어다니며 물풍선을 놓고, 시간이 지나면
맵 외곽이 잠겨 중앙으로 몰리는 상시 오픈 필드 서버.

**서버 권위(server-authoritative)** 구조다. 클라이언트는 입력만 보내고,
이동 유효성·폭발 판정·피격은 전부 서버가 결정한다.

### 왜 이 주제인가

| 요소 | 얻는 것 |
|---|---|
| 격자 이동 + 물풍선 판정 | 서버 권위 시뮬레이션, 틱 기반 루프 |
| 30인 동시 접속 | 세션 관리, **AOI 브로드캐스트 최적화** ← 이 프로젝트의 핵심 |
| 존 축소 | 전역 상태 동기화, 후반 밀집으로 인한 최악 부하 케이스 생성 |
| 부하 측정 | 자소서 2번이 요구하는 "결과와 함께"의 숫자 |

---

## 2. 범위

### 반드시 만든다

- IOCP 기반 비동기 TCP 서버, 워커 스레드 풀
- 세션 관리 (접속 / 정상 종료 / 강제 절단)
- 직접 구현한 링버퍼 + 고정 헤더 패킷 프로토콜
- 30Hz 틱 루프, 서버 권위 게임 시뮬레이션
- 격자 이동 / 물풍선 설치 / 타이머 폭발 / 십자 판정 / 피격·부활
- 존 축소 타이머
- AOI 셀 분할 브로드캐스트
- 더미 클라이언트 부하 테스트 도구 + 측정 리포트

### 여유 되면

- 클라이언트 입력 예측 + 서버 보정 (reconciliation)
- 웹 관전 뷰어 (Node WebSocket 브리지)

### 손대지 않는다

DB, 계정·인증, 매칭메이킹, 암호화, 프로토콜 버전 관리, 엔진 연동, 예쁜 UI.
전부 이 9일에 넣으면 아무것도 안 끝난다.

---

## 3. 프로토콜 (초안)

고정 헤더 + 페이로드. 리틀 엔디안.

```
[ uint16 size ][ uint16 packetId ][ payload ... ]
size = 헤더 포함 전체 바이트
```

| 방향 | ID | 이름 | 페이로드 |
|---|---|---|---|
| C→S | 1 | ENTER | nickname |
| C→S | 2 | MOVE | dir(u8), seq(u32) |
| C→S | 3 | PLACE_BUBBLE | seq(u32) |
| S→C | 101 | ENTER_ACK | myId, mapW, mapH, tickRate |
| S→C | 102 | SPAWN | id[], x[], y[] (AOI 진입) |
| S→C | 103 | DESPAWN | id[] (AOI 이탈) |
| S→C | 104 | STATE | tick, (id, x, y)[] |
| S→C | 105 | BUBBLE_PLACED | id, x, y, fuseTick |
| S→C | 106 | BUBBLE_EXPLODED | x, y, range, hitIds[] |
| S→C | 107 | ZONE_SHRINK | newBounds |

`seq`는 나중에 입력 예측·보정을 붙일 때 쓴다. 지금은 받아서 되돌려주기만.

---

## 4. 9일 계획

각 날의 **완료 조건**을 통과해야 다음 날로 넘어간다.
통과 못 하면 그날 밤에 다음 날 범위를 줄인다. 밀린 채로 진행하지 않는다.

### D-11 · 8/27 (목) — 세팅
- VS2022 확인, 솔루션 생성, Git 레포 Public 생성
- `.gitignore` 먼저 커밋 → 그다음 코드 (순서 중요)
- `CLAUDE.md`, `SPEC.md` 배치
- blocking TCP 에코 서버 + 클라이언트 (각 파일 하나)
- **완료 조건:** 클라이언트에서 친 문자열이 되돌아온다

### D-10 · 8/28 (금) — IOCP 진입
- 오전: `select` 기반 다중 접속으로 논블로킹 개념 체득
- 오후: IOCP 전환 — 완료 포트 생성, 워커 1개, `WSARecv` 1회전
- **완료 조건:** IOCP로 에코가 동작한다. `OVERLAPPED`가 왜 필요한지 말로 설명할 수 있다

### D-9 · 8/29 (토) — 세션
- `Session` (소켓 + 버퍼 + 상태), `SessionManager`
- `OVERLAPPED` 확장 구조체로 Recv/Send 구분
- 강제 종료·half-close 처리, 참조 카운트
- **완료 조건:** 클라 10개를 붙였다 랜덤하게 끊어도 서버가 죽지 않는다

### D-8 · 8/30 (일) — 버퍼와 패킷
- 링버퍼 직접 구현 (Recv / Send)
- 헤더 파싱, **분할 수신 / 연접 수신** 처리
- **완료 조건:** 1바이트씩 쪼개 보내도, 여러 패킷을 한 번에 몰아 보내도 안 깨진다
- ※ 이 날의 버그가 자소서 2번 소재 1순위다. 로그를 특히 자세히

### D-7 · 8/31 (월) — 멀티스레드
- 워커 스레드 풀(N개), Job Queue
- 락 설계 — 세션별인가 룸별인가, 무엇을 누가 소유하는가
- 브로드캐스트 경로
- **완료 조건:** 스레드 4개에서 동시 접속·이탈·브로드캐스트가 레이스 없이 돈다.
  각 자료구조의 소유 스레드를 주석으로 답할 수 있다

### D-6 · 9/1 (화) — 게임 로직
- 격자 맵, 이동 유효성, 물풍선 설치, 퓨즈 타이머, 십자 폭발, 피격·부활
- 30Hz 틱 루프
- **완료 조건:** 클라이언트 없이 로그만으로 시뮬레이션이 정상 동작한다

### D-5 · 9/2 (수) — ★ 체크포인트
- 콘솔 클라이언트로 실제 2인 플레이
- 존 축소 타이머
- **완료 조건:** 두 명이 붙어서 물풍선으로 서로를 잡을 수 있다
- **판정:** 이날 밤까지 위가 안 되면 게임 로직을 버리고 채팅·룸 서버로 축소한다.
  그리고 D-4부터 서류로 전환한다. 미련 없이 자른다

### D-4 · 9/3 (목) — ★ 차별화
- 더미 클라이언트 부하 테스트 도구 (C구역 — AI에 위임)
- **1차 측정:** 봇 30개, 전원 브로드캐스트 → 초당 패킷 수 / 대역폭 / CPU / 평균 지연
- **AOI 구현:** 맵을 셀로 분할, 인접 9셀만 브로드캐스트, 진입·이탈 시 SPAWN/DESPAWN
- **2차 측정** → 전후 비교표
- **완료 조건:** 전후 수치가 표로 나온다. 이 표가 자소서 2번과 포트폴리오의 중심이다

### D-3 · 9/4 (금) — ★ 기능 동결
- 이후 새 기능 금지. 정리만
- 웹 관전 뷰어 + 브리지 (C구역)
- 시연 영상 촬영 — 봇 30개 + 존 축소 + AOI 시각화
- 코드 정리, 구조도, README
- 9일치 개발 로그 정리
- **완료 조건:** 처음 보는 사람이 README만 읽고 빌드해서 돌릴 수 있다

### D-2 · 9/5 (토) — 서류 1
자소서 4문항, 프로젝트 이력, 자유설문 입력

### D-1 · 9/6 (일) — 서류 2
포트폴리오 PDF, 전체 검수, **제출**

### D-0 · 9/7 (월)
예비일. 16:00 전 제출 상태 확인만

---

## 5. 측정 항목 (D-4에서 채운다)

| 지표 | 전 (전원 브로드캐스트) | 후 (AOI) |
|---|---|---|
| 봇 30, 초당 송신 패킷 수 | | |
| 초당 송신 바이트 | | |
| 서버 CPU 사용률 | | |
| 입력→반영 평균 지연 (ms) | | |
| 최대 안정 동시 접속 | | |

측정 조건(맵 크기, 봇 행동 패턴, 측정 시간, 머신 사양)을 README에 함께 적는다.
조건 없는 숫자는 면접에서 반드시 되물린다.

---

## 6. 매일 남길 개발 로그

`devlog/YYYY-MM-DD.md`, 세 줄.

```
1. 오늘 하려던 것:
2. AI에게 물은 것 / 어떻게 물었나:
3. AI가 틀린 것 / 내가 어떻게 잡았나:
```

3번이 비는 날이 있으면 그날은 A구역을 안 건드린 날이다.
이 파일들이 10월 3일 AI 활용 역량평가의 재료이고, 포트폴리오 마지막 섹션이다.
