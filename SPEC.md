# Bubble Royale Server — 스펙 & 빌드 계획 (rev.2)

C++ / IOCP 기반 실시간 멀티플레이 게임 서버.
크레이지아케이드의 조작감을 **24인 라운드제 배틀로얄**로 옮긴 것.

- 개발 기간: 2026.08.27 ~ 09.05 · 기능 동결 9/5 · 제출 9/6
- 환경: Windows 11 / Visual Studio 2022 / C++17 / x64 / WinSock2 + IOCP
- 클라이언트: **웹 (Canvas)** ← Node WebSocket 브리지 ← C++ 서버
- 부하·지연 측정: C++ 더미 클라이언트가 서버에 **직접** 붙음 (브리지 우회)

---

## 1. 설계 원칙

이 프로젝트는 **게임 디자인 결정이 서버 설계 요구로 번역되는 과정**을 보여주는 것이 목적이다.
모든 상수와 구조 선택에는 "왜"가 있어야 하고, 그 "왜"는 게임의 재미에서 출발한다.

| 게임에서 정한 것 | 서버가 해야 하는 것 |
|---|---|
| 걸치기(픽셀 단위 정밀 회피)가 핵심 조작 | 위치를 **고정소수점 연속값**으로, 판정은 타일 단위로 분리 |
| 서버·클라가 같은 판정을 내야 함 | float 금지. 정수 연산만 (결정론) |
| 연속 이동이라 위치를 매 틱 보내야 함 | **AOI가 필수가 됨** — 이 프로젝트의 핵심 최적화 |
| 탄막 회피 | 입력→반영 **100ms 예산**. 클라이언트 예측 필요 |
| 침수 예고가 전원에게 보여야 함 | 전역 브로드캐스트 채널을 AOI와 **별도로** |
| 구역 넘어가자마자 죽으면 억울함 | **엔티티 타입별 전파 범위** (아래 4절) |
| 카메라가 구역 단위로 전환 | AOI 셀 = 맵 조각 = 15×13. 보내는 것과 보이는 것이 일치 |
| 경계에서 카메라 떨림 방지 | 히스테리시스 (판정 타일 전환에도 같은 개념 적용) |
| 맵 조각 10종 랜덤 배치 | 데이터 주도 로딩 + **경계 통로 규칙** |

---

## 2. 게임 규칙

### 2.1 판 구성

- **24명**, 라운드제. 목표는 최후 1인
- 경기 시간 6~7분 (10분 도달 시 무승부)
- 사망 = 탈락, 관전 모드로 전환

**판은 네 단계를 돈다.**

```
기다림 ──(2명 이상)──> 카운트다운(3초) ──> 진행 ──(1명 남음)──> 결과(5초)
   ^                                                              │
   └──────────────────── 새 판을 깔고 다시 앉힌다 ──────────────────┘
```

라운드제로 도는 이유는 **죽은 뒤의 시간** 때문이다.
24인 배틀로얄에서 1분에 죽으면 남은 시간이 통째로 빈다. 크아가 라운드제인 것도 같은 이유다.
죽어도 곧 다음 판이 온다.

- **관전은 따로 만들 게 없었다.** 스냅샷이 어차피 전원에게 나가므로,
  죽은 사람도 판을 그대로 보고 있다. 화면에 "관전 중" 만 띄우면 된다
- **자리가 없어도 안 끊는다.** 25번째 사람은 보고 있다가 다음 판에 앉는다
- **판이 도는 중에 들어오면 그 판은 관전이다.** 남들이 파밍한 판에
  빈손으로 끼워 넣으면 들어오자마자 죽는다

### 2.2 맵

- 맵 조각(sector) = **15 × 13** (크레이지아케이드 원본 크기)
- 전체 맵 = 조각 **3 × 3 = 9개** → **45 × 39**
- 조각은 손으로 그린 **10종 중 9개를 랜덤 선택·배치**하고, 자리마다 **뒤집기**를 따로 건다
  (`Server/src/SectorTemplates.h`)
- 조각 글자: `#` 고정 벽 / `?` 상자 자리 / `.` 조각이 그린 길 / `s` 스폰
- 상자 다섯 중 하나는 **밀 수 있는 상자**다 (`BOX_PERCENT`).
  블록은 부수는 것 말고 할 게 없는데, 상자는 밀 수 있어서 **같은 벽 하나에 선택지가 둘**이 된다.
  부수면 2.5초 걸리고 아이템이 나올 수도 있다. 밀면 즉시지만 민 자리에 그대로 있다.
  통로를 막을 수도, 물에 밀어 넣을 수도, 쫓기면서 뒤로 밀어 길을 끊을 수도 있다.
  뒤가 막혔거나 사람이 서 있으면 안 밀린다. 한 번 밀면 `PUSH_COOLDOWN_TICKS` 쉰다 —
  안 그러면 붙어서 누르는 동안 상자가 주르륵 밀려간다
- **9/1 부터 고정 벽과 스폰 자리 빼고 전부 상자다** (`BLOCK_FILL_PERCENT = 100`).
  50% 일 때는 시작하자마자 길이 뚫려 있어서 서로 30초 만에 만났다.
  크아도 봄버맨도 시작은 상자에 둘러싸인 채다. 그게 앞의 1분을 만든다.
  조각이 그린 길(`.`)도 상자로 덮이고, **파고 나면 드러난다**
- 조각이 지키는 약속 — `tools/maptest.exe` 가 맵 200개를 매번 다시 잰다
  - 관문 `(7,0) (7,12) (0,6) (14,6)` 은 길이다. 좌우로 뒤집으면 좌우 관문이,
    위아래로 뒤집으면 위아래 관문이 자리를 바꾼다. **어느 쪽으로 뒤집어도 관문은 관문이다**
  - **길(`.`)만으로 관문 넷과 스폰 셋이 전부 이어진다.** 블록이 최악으로 깔려도 시작하자마자 안 갇힌다
  - 스폰은 `x 3..11, y 3..9` 안에만. 경계에 붙이면 옆 조각 스폰과 세 칸이 되는데,
    물줄기 사거리의 세 배는 떨어져야 시작하자마자 사정권이 아니다. 지금 제일 가까운 두 스폰은 **6칸**
  - 벽으로 갈라진 칸이 없다
- 붙이고 나서 판이 고치는 것 (`GameMap::Generate`)
  - 판 테두리를 두르면서 생긴 **막다른 칸을 벽으로 메운다.** 뚫지 않고 메우는 이유는
    이웃이 하나뿐인 칸은 지나다니는 길이 아니라 들어갔다 그대로 나와야 하는 끝이라,
    없애도 어디와 어디 사이가 안 끊기기 때문이다. 뚫으면 조각에 그린 모양이 망가진다
  - 스폰 주변 3x3 을 비운다. 이 크기여야 한다 — 가운데에 물풍선을 놓으면
    사거리 1 물줄기가 십자로 덮고 **네 귀퉁이가 남는다.** 대각선으로 피할 수 있다
  - 스폰 주변 상자 수를 **제일 적은 쪽에 맞춰 내린다.** 평균에 맞추면 위로 못 올린다
    (100% 로 채워서 채워 넣을 빈칸이 없다). 아래로만 맞추면 언제나 된다
  - 물풍선을 놓으면 무조건 죽는 칸을 없앤다
  - 위 둘은 **번갈아 두 번** 돈다. 둘 다 상자를 지우는 일이라 서로를 망가뜨린다.
    공정성을 먼저 하면 죽는 칸 없애기가 다시 어긋내고, 반대면 지운 자리가 새 죽는 칸이 된다
- 24명을 27개 스폰 자리 중 랜덤 배치 (**차 있는 스폰에서 제일 먼 자리**부터)
- 시작 시 **3초 카운트다운**

**왜 손으로 그리나.** 확률로만 만들 때도 숫자는 다 맞았다. 밀도도, 탈출 거리도, 공정성도.
그런데 어디를 가도 같은 데 같았다. 크아 맵이 기억에 남는 건 밀도가 좋아서가 아니라
**광장과 골목과 문턱이 있어서**다. 그래야 "거기" 라고 부를 수 있다.
그래서 조각 열 개에 각각 성격을 줬다 — 십자 광장, 회랑, 톱니, 격자, 방 네 개,
대각선, 골목, 우물, 지그재그, 부두.

### 2.3 이동과 걸치기 — 이 게임의 핵심

크레이지아케이드의 "걸치기": 두 타일 경계에 서면
**피격 판정이 먼저 있던 쪽으로 적용되어 물줄기에 닿아도 안 죽는다.**

구현 모델:

- **위치는 연속** — 고정소수점. 타일 하나를 **256 단위**로 분할
- **판정은 한 줄** — **몸의 과반수가 있는 칸이 내가 있는 칸이다.**
  몸이 중심 기준으로 대칭이라 그건 곧 중심이 있는 칸이고, 나눗셈 한 번이면 끝난다
- **몸은 타일보다 작다** — `PLAYER_BODY_NUM/DEN` (9/1 부터 **0.80**). 그래서 **두 칸에 걸쳐 설 수 있다**
- **걸치기는 여기서 나온다** — 물줄기는 칸 단위로 덮는다.
  몸은 물에 닿았는데 중심은 안전한 칸에 있는 순간이 생긴다. 그 순간이 걸치기다
  - 서버만 이걸 안다. 그래서 `EVT_GRAZE` 를 따로 보낸다 (2.7 참고)
  - 이 상수는 판정을 바꾸지 않는다. **걸쳐 있다는 게 얼마나 눈에 보이는지**만 정한다
- **속도의 자연스러운 트레이드오프** — 빠를수록 경계에 정밀하게 멈추기 어려움.
  별도 페널티 없이 규칙에서 저절로 나온다

**9/1: 몸이 벽에는 안 걸친다.**

전에는 **몸 중심**이 들어가려는 칸만 봤다. 몸이 0.8 이라 벽에 0.4 만큼 파묻힌 채로 설 수 있었다.
이제 **가는 쪽 몸 끝**을 본다. 몸 끝이 벽 칸에 들어가려 하면 경계 바로 앞에서 선다.

걸치기는 안 없어진다. 걸치기는 **물줄기**에 대해 일어나는 것이고 물줄기는 빈 칸에만 깔린다.

> **대각선이 비어 있었다.** 중심이 있는 칸 하나만 보면, 오른쪽으로 가는데 몸이
> 위아래 두 줄에 걸쳐 있을 때 들어가려는 칸 **위쪽** 벽에 몸 귀퉁이가 박힌다.
> 격자 맵에서 6000틱 중 **1775틱**이 그랬다. 눈으로는 "벽에 파묻힌 채로 다닌다".
> 그래서 반대 축으로 몸이 걸친 칸을 **전부** 본다 (`movetest` 시험 9가 이걸 잰다).

**코너 보정을 껐다. 대신 줄 맞춤을 넣었다.**

| | 무엇을 하나 | 왜 뺐나 / 왜 넣었나 |
|---|---|---|
| 코너 보정 | **옆 칸으로** 민다 | 벽에 조금만 비벼도 대각선으로 미끄러진다. 원하는 칸에 못 선다 |
| 줄 맞춤 | **내 칸 한가운데로** 당긴다 | 줄을 절대 안 벗어난다. "벽에 닿으면 선다" 를 지킨다 |

코너 보정을 끄고 나니 통로를 못 지나가게 됐다. 통로가 한 칸(256)인데 몸이 204 라,
지나가려면 중심이 **가운데 52칸(20%)** 안에 있어야 한다. 키보드로 그건 못 맞춘다.

**언제 당기는지가 절반이다.** 처음에는 한 방향만 누르면 늘 당겼더니
트인 데서 걷는데도 자꾸 옆으로 밀렸다. 그래서 조건을 둘로 좁혔다.

| 조건 | 왜 |
|---|---|
| 가려는 칸이 **뚫려 있어야** 한다 | 벽에 대고 누르는 동안 옆으로 흐르면 그건 미끄러짐이다. **막혔으면 선다** |
| 가려는 칸의 **옆이 막혀 있어야** 한다 | 트인 데서는 줄을 맞출 이유가 없다 |

줄 맞춤은 **내 칸 안에서만** 당기므로 줄이 안 바뀐다.
줄을 바꾸는 일은 여전히 사람이 직접 누른다. 이 게임은 물풍선을 어느 칸에 놓을지가
판단의 전부라, 서고 싶은 데 못 서면 게임이 무너진다.

- 클라이언트에 **판정 타일 표시** — 발밑에 아주 희미한 테두리. 눈에 걸리면 안 된다.
  기본은 최소 밝기로 두고, 시연 영상 촬영 때만 진하게 올려 메커니즘을 설명한다
- 클라이언트는 캐릭터를 **서버가 준 몸 크기 그대로** 그린다. 실제 크기로 그려야
  두 칸에 걸쳐 서 있다는 게 눈에 보인다

> **처음에는 판정 타일을 따로 두고 늦게 따라오게 만들었다가 8/31에 갈아엎었다.**
> 새 칸에 68% 들어가야 판정이 옮겨가는 방식이었는데, 그러면 발밑이 아니라
> 방금 지나온 칸에 물풍선이 깔린다. 걸치기는 판정을 늦추는 게 아니라
> **몸이 칸보다 작아서** 생기는 것이다.

### 2.4 물풍선

- 설치 → 퓨즈 → 십자 폭발 (길이는 물줄기 스탯만큼)
- **연쇄 폭발** — 폭발이 다른 물풍선에 닿으면 그 물풍선의 퓨즈를 `CHAIN_STEP_TICKS` 로 줄인다.
  **즉시 터뜨리지 않는다.** 한 틱에 다 터지면 큰 폭발 하나로 보여서 연쇄인 줄 모른다 (2.7의 2위).
  늦추면 번져나가는 게 보이고, 재귀도 방문 표시도 필요 없어지고, 무한루프가 아예 안 생긴다
- 폭발은 파괴 가능 블록에서 멈춤. 블록은 파괴됨
- **자기 풍선** — 놓은 칸에서는 나갈 수 있고, 나가면 다시 못 들어감
- **갇힘** — 물줄기에 맞으면 물방울에 갇힌다. **기어가듯 아주 느리게만 움직인다**
  (`TRAP_MOVE_SPEED`, 기본의 1/4). 아예 묶어두면 7초가 그냥 죽은 시간이 되고,
  느리게라도 갈 수 있어야 "물줄기 밖으로 기어나갈까" 가 판단거리가 된다
  - **마무리는 몸으로만 된다.** 물줄기로는 갇힌 사람을 더 어쩌지 못한다.
    다른 사람이 몸으로 부딪쳐야 터진다. 크아가 그렇다
  - → 그래서 마무리하러 가는 것 자체가 위험을 진다. 거리를 좁혀야 하니까.
    SPEC 2.7 이 말한 "잡으러 갈까" 가 여기서 진짜 판단거리가 된다
  - → 갇힌 쪽도 기어서 도망칠 수 있으니 (`TRAP_MOVE_SPEED`) 쫓고 쫓기는 7초가 된다
  - **7초**를 버티면 스스로 탈출 (짧은 무적).
    5초는 너무 빨리 풀렸다. 잡으러 가는 쪽이 거리를 좁히기도 전에 끝났다
  - **바늘은 자동 소모가 아니라 플레이어가 눌러야 쓰인다.** 안 쓰면 그냥 7초를 버틴다
  - → 갇힌 7초가 기다리는 시간이 아니라 **판단하는 시간**이 된다.
    주변에 적이 없으면 아끼고, 연쇄가 올 것 같으면 즉시 사용. 판단 실수로 죽는다
  - → 연쇄 폭발은 **가두는 수단**이지 마무리 수단이 아니다.
    여러 명을 한꺼번에 가둘 수는 있어도, 터뜨리려면 결국 몸으로 가야 한다

### 2.5 아이템

**수치형 — 벽에서 나옴. 누적.**

| 아이템 | 효과 | 시작값 | 상한 |
|---|---|---|---|
| 물풍선 | 동시 설치 개수 +1 | 1 | 5 |
| 물줄기 | 폭발 길이 +1 | **1** | 5 (울트라로 7) |
| 롤러 | 이동 속도 +1 | 0 | **7** |

> **9/1 에 시작 사거리를 2 에서 1 로 낮췄다.** 사방 한 칸이면 첫 물풍선이 거의 아무도 못 죽인다.
> 앞의 1분이 진짜 파밍 구간이 되고, 물줄기 아이템 하나하나가 체감된다 (1→2 는 사거리가 두 배다).
>
> **롤러만 상한이 7 이다.** 상자를 100%로 채우고 나서 판이 넓게 느껴졌는데
> 끝까지 먹어도 4.0 타일/초라 가로지르는 데 너무 오래 걸렸다.
> 롤러가 제일 체감이 큰 아이템이어야 한다. 지금은 5.4 타일/초까지 간다.

**특수형 — 안 넣는다. 9/2 에 잘라냈다.**

> 아래 다섯은 **설계만 있고 코드에는 없다.** 9/4 기능 동결까지 안 넣는다.
> 규칙이 다섯 개 늘면 설명할 것이 다섯 개 늘고, 봇이 그걸 못 쓴다.
> 봇이 못 쓰는 규칙은 밸런스 시뮬레이션에 안 잡히므로,
> 넣어놓고 안 재본 것이 되어 오히려 나빠진다.
> 설계를 지우지 않고 남겨두는 이유는 **왜 안 넣었는지가 질문이 되기 때문**이다.

| 아이템 | 효과 | 비고 |
|---|---|---|
| 신발 | 설치된 물풍선을 밀어서 굴림 | 변칙 공격의 핵심 |
| 유령 | 잠시 투명 | **AOI 예외 — 가까워도 안 보냄** |
| 레이더 | 잠시 인접 구역 플레이어 위치 표시 | **AOI 예외 — 멀어도 보냄** |
| 드릴 | 고정 블록 1개 파괴 | 침수 구역 탈출로 확보 |
| 바늘 | 갇힘 즉시 탈출 | **수동 발동.** 쓸지 아낄지 선택 |

**발동 방식(설계)** — 신발만 패시브(이동 중 자동으로 밀림)이고,
**유령 · 레이더 · 드릴 · 바늘은 전부 발동형**. 키 하나 + 아이콘 하나로 UI가 끝난다.
`USE_ITEM` 패킷을 공유하며, 서버가 사용 가능 상태인지 검증한다
(예: 바늘은 갇힘 상태에서만 유효).

**`USE_ITEM` 패킷은 프로토콜에 없다.** 넣는 날 같이 만든다.

**드롭 규칙 (이 한 줄이 밸런스의 척추)**

> 벽에서 나오는 건 수치형뿐이고 상한은 4 (롤러만 7).
> 상한 4 이상(울트라)은 **킬 드롭과 중앙 위험 구역에서만** 나온다.
>
> 특수 아이템을 뺐으므로 지금 이 척추를 지고 있는 것은 **울트라 하나뿐**이다.

- 파밍만으로는 구조적으로 만렙 불가
- 숨어 다닌 플레이어는 특수 아이템을 아예 못 봄
- 특수 아이템이 **후반에 등장** → 밀집 구간이 가장 변칙적인 구간이 됨
- **킬 드롭** — 사망 시 보유 아이템의 절반을 주변 타일에.
  **올림으로 흘린다.** 하나 가진 사람을 잡아도 하나는 나와야 잡을 맛이 난다.
  이게 없으면 사람을 잡을 이유가 "하나 줄었다" 뿐이라 아무도 안 찾아다닌다
- **울트라** — 물줄기를 `STAT_CAP_ULTRA`(6) 로 한 번에 올린다.
  벽에서는 **절대** 안 나온다. 킬 드롭(`ULTRA_DROP_PERCENT`)과 최종 보급에서만 나온다
- 아이템은 블록에 미리 심지 않고 **부술 때 확률로 결정** (맵 조각별 편차 제거)

봇 20판에서 **살아남은 사람이 상한 15 중 15.0 개**를 들고 끝난다 (9/2 측정).
스노볼이 실제로 돈다는 뜻이고, 그래서 울트라 같은 역전 장치가 한 세트로 묶여 있어야 한다.

**최종 구역 보급** — 최종 1구역이 되는 순간 정중앙에 **울트라 하나**.
하나뿐이고 위치가 전원에게 보이므로, **먹으러 가는 것 자체가 위험**하다.
그 과정에서 교전이 강제되는 것이 이 보급의 목적이다.

> 원래는 거북이 / 해적 거북이 / 바늘 중 랜덤 하나였다.
> 탈것과 1회 피격 방어는 이동·피격 판정을 따로 만들어야 해서 지금은 울트라로 대신한다.
> 목적(중앙으로 끌어내 교전을 강제한다)은 그대로다.

**무승부 방지** — 원래는 8분 뒤 황금 물풍선이었는데, **최종 구역 안 수위 상승**(2.6)이
그 역할을 대신한다. 판이 7x5 까지 좁아지므로 교착이 구조적으로 안 생긴다.
봇 20판에서 무승부 0판.

### 2.6 침수 (존 축소)

레드존이 아니라 **물이 차오르는** 연출. 세계관 일관성.

| 시각 | 일 |
|---|---|
| 0:00~1:00 | 자유 파밍 |
| 1:00 | **1차 예고 (3구역)** → 1:30 침수 |
| 2:30 | 2차 예고 (3구역) → 3:00 |
| 4:00 | 3차 예고 (2구역) → 4:30 |
| 4:30~ | 최종 1구역 = **15×13, 크아 원본 크기**에서 결착 |

**예고는 항상 침수 30초 전이다.** 그 30초가 도망칠지 버틸지 정하는 시간이다.
바깥 여덟 구역이 3+3+2 로 잠긴다. **구석 넷이 먼저, 변 넷이 나중이다.**

> 처음에는 여덟 개를 통째로 섞었다. 그랬더니 변 두 개가 먼저 잠기면서
> 그 사이 구석이 통째로 고립되는 판이 나왔다. 거기 있던 사람은 실력과 상관없이 갇혀 죽는다.
> 구석은 이웃이 둘뿐이라 먼저 잠겨도 남는 것이 안 끊긴다. 무리 안에서만 섞는다.

**최종 구역은 끝이 아니라 시작이다.** 구역을 다 잠근 뒤에도 그 안에서 계속 차오른다.

```
15x13 ──20초──> 13x11 ──> 11x9 ──> 9x7 ──> 7x5 에서 멈춘다
143칸           ~100      ~70      ~45     35칸
```

이게 없으면 판이 안 끝난다. 계산은 이렇다.

> **사람은 칸보다 빨리 준다.**
> 침수는 칸을 9→6→3→1 로 줄인다. 총 ×0.11.
> 사람은 24→1 로 준다. ×0.04.
> 그래서 후반으로 갈수록 **오히려 헐렁해진다.**

최종 1구역 143칸에 넷이 남으면 한 명당 36칸이다. 크아의 두 배라 서로 못 만난다.
봇 20판에서 10분 무승부가 나던 게 그것 때문이었고, 안쪽 물을 넣으니 **0판**이 됐다.
7x5 면 둘이 남았을 때 한 명당 12칸이라 반드시 만난다.

- 안쪽 물에는 **예고가 없다.** 눈에 보이는 것이 예고다
- 판정 기준은 구역 침수와 같다. **판정 칸이 물 밖이면 산다** — 경계에서 걸치기가 통한다

- 어느 구역이 닫히는지는 매판 랜덤. 바깥쪽부터
- **즉사 없음.** 침수 순간 그 안에 있으면 화면 붉어지며 **2초 카운트다운** → 못 나가면 사망
- **진입은 막지 않는다.** 막으면 갇혀서 억울해짐
- 부수 효과: 2초면 대여섯 칸. **침수 구역을 지름길로 가로지르는 하이리스크 플레이**가 생김
- **잠겨도 아이템은 안 사라진다.** 사라지면 들어갈 이유가 지름길뿐이라 약하다.
  남겨두면 "2초 안에 저것만 먹고 나올까" 가 판단거리가 된다
- 연출: 예고 중 바닥에 물이 얕게 깔리며 붉은 경고 테두리 점멸.
  임박할수록 빠르게. 인접 구역에는 그 방향 화면 가장자리에 붉은 느낌표
- 화면 상단에 얇은 타이머 바

---

### 2.7 어디서 재미가 나오나

클리커 게임이 클릭 하나에 이펙트를 몰아주듯, 이 게임도 몰아줄 데를 정해야 한다.
아래 순서가 연출 예산의 배분 순서다. **1위에 아끼면 이 게임을 만든 이유가 사라진다.**

**1위 — 걸치기로 살아남은 0.23초**

이 게임에서 유일하게 "내 손이 좋아서 살았다"가 성립하는 지점이다.
나머지는 판단이거나 운이다.

문제는 **살아남은 걸 플레이어가 모른다**는 것이다. 물줄기가 발밑을 지나갔는데
아무 일도 없으면 위험했다는 사실 자체를 모른다. 메커니즘은 있는데 리턴이 0이다.

→ **서버가 `EVT_GRAZE` 를 따로 보낸다.** 몸이 있는 타일은 맞았는데 판정 타일이
안 맞았다는 건 서버만 안다. 클라이언트가 추론할 수 없다.
**게임 필 요구가 서버 패킷을 하나 늘린 경우다.** 1절 표에 들어갈 항목이다.

**2위 — 연쇄 폭발**

설치의 보상은 폭발이 아니라 연쇄다. 하나 터지는 건 예상한 결과고,
연쇄는 예상 밖이라 리턴이 된다.

→ **연쇄 한 단계마다 `CHAIN_STEP_TICKS` 만큼 늦춘다.** 한 틱에 다 터뜨리면
큰 폭발 하나로 보여서 연쇄인 줄 모른다. 번져나가는 게 보여야 한다.
죽은 사람도 그 장면은 끝까지 본다.

**3위 — 갇힌 7초**

2.4에 적은 대로 기다리는 시간이 아니라 판단하는 시간이다. 리턴이 셋으로 갈린다.

| 누가 | 무엇을 느껴야 하나 |
|---|---|
| 갇힌 사람 | 조여드는 긴장. 마지막 1초는 눈에 띄게 다른 상태 |
| 주변 사람 | **초대장.** "잡으러 갈까" 가 떠올라야 한다 |
| 구하러 간 사람 | 늦었으면 늦은 게 보여야 한다 |

→ 주변에게 초대장이 되려면 멀리까지 알려야 한다. **갇힘은 AOI 예외 후보다.**

**4위 — 퓨즈 2.5초** — 설치 후 아무 일도 없는 이 구간이 체감상 제일 길다.
등속으로 깜빡이면 시간이 안 느껴진다. 마지막 0.5초에 맥박이 빨라져야 한다.

**5위 — 침수 구역 가로지르기** — 2.6의 2초는 대여섯 칸이다. 성공하면 그 판에서
제일 기억에 남는 장면이 된다. 아깝게 못 나가는 게 보여야 다음에 또 시도한다.

**6위 — 수치 아이템** — 작고 잦다. **여기는 연출을 줄인다.**
먹을 때마다 화면이 번쩍이면 3분 뒤에 피로해진다.
대신 특수 아이템은 크게 한다. 대비가 있어야 특수가 특별해진다.

### 2.8 화면에 무엇을 띄우나

**판 위에는 글자를 안 띄운다.**

처음에는 `GRAZE`, `CHAIN x3` 같은 짧은 영어를 띄우기로 했다가 8/31에 갈아엎었다.
직접 돌려보니 두 가지가 걸렸다.

- 판 위에 영어가 떠다니면 화면이 시끄럽고 싸구려로 보인다
- **글자는 읽어야 알지만 그림은 안 읽어도 안다.** 싸우는 중에 글자를 읽을 여유가 없다

**글자를 쓰는 곳은 판 밖뿐이고, 전부 한글이다.**
카운트다운은 숫자만 크게, 킬 피드는 색 점과 `P3 ▸ P5` 로만 쓴다.

### 2.9 아트 디렉션 — 다섯 줄이 나머지를 전부 정한다

**캐릭터는 동물 여덟 종이다.** 고양이 · 강아지 · 토끼 · 곰 · 여우 · 판다 · 개구리 · 병아리.

둥근 머리에 색만 다르면 스물넷이 다 같은 인형으로 보인다.
크아가 캐릭터로 기억되는 건 다오와 우니가 **다르게 생겨서**다.

색이 24개, 동물이 8종이고 **주기가 다르다.**
색이 같은 사람끼리는 동물이 다르고, 동물이 같으면 색이 다르다.

가르는 순서는 **실루엣이 먼저다.** 귀 모양이 제일 크게 다르고,
그것만 안개 너머와 작은 표에서도 읽힌다. 주둥이와 눈은 가까이서만 보이는 두 번째다.

킬 피드와 결과표에도 번호 대신 **얼굴**을 그린다. 판에서 쓰는 것과 같은 함수로 그린다.
"P3" 은 번호고 얼굴은 그 사람이다. 따로 그리면 언젠가 갈린다.


9/1에 화면을 통째로 다시 만들었다. 그 전 화면은 규칙은 다 보였지만
**"웹에서 만든 것" 처럼 보였다.** 원인을 다섯 개로 좁혔고, 그게 그대로 규칙이 됐다.

| | 규칙 | 안 지키면 |
|---|---|---|
| ① | **빛은 왼쪽 위 한 군데서만 온다.** 밝은 면이 전부 그쪽을 보고 그림자가 오른쪽 아래로 진다 | 따로 그린 것들이 한 판 안에 있어 보이지 않는다 |
| ② | **평평한 것을 두지 않는다.** 벽에 높이가 있다. 윗면·앞면·바닥 그림자가 따로 있다 | 타일에 색만 칠한 지도가 된다 |
| ③ | **명도 순서를 못 박는다.** 벽 앞면 < 바닥 < 벽 윗면 < 상자 < 캐릭터 | 스물넷이 엉키면 사람이 안 보인다 |
| ④ | **등속으로 움직이는 것을 두지 않는다.** 전부 가감속, 튀어나오는 것은 살짝 지나쳤다 돌아온다 | 값이 바뀌는 것이 보일 뿐 움직이는 것으로 안 보인다 |
| ⑤ | **모든 입력에 0.1초 안에 답한다.** 그림 + 소리 + 화면 흔들림 | 규칙이 맞아도 손맛이 없다 |

**깊이 정렬.** 판을 한 장으로 그리지 않고 **줄마다 따로 그려서 사이에 사람을 끼운다.**
아래 줄의 벽이 위 줄의 사람을 가린다. 그게 "앞에 있다" 는 뜻이다.
한 장에 그려두면 사람이 늘 벽 앞이거나 늘 벽 뒤가 된다.

**카메라.** 손맛의 절반이 여기 있다.

- **흔들림** — 세기를 바로 넣지 않고 쌓았다가 **제곱해서** 쓴다.
  선형으로 하면 자잘한 폭발이 겹칠 때 화면이 멀미 나게 떨린다
- **멈춤(hit stop)** — 사람을 잡은 순간 70ms 동안 시간이 안 흐른다.
  맞은 게 아니라 **맞혔다**는 걸 몸으로 알리는 장치다
- **번쩍임** — 걸치기와 마무리에만. 아껴 써야 효과가 남는다

**구역마다 다른 장소다.** 크아 맵이 기억에 남는 건 빌리지, 캠프, 해변처럼
**거기가 어디인지 알아서**다. 우리 판은 조각 아홉 개를 붙인 것이니 조각마다 다르게 그린다.
조각 열 종에 장소 열 곳이 붙어 있다 — 광장 · 사원 · 공장 · 마을 · 캠프 · 사막 ·
시장 · 해변 · 얼음골 · 부두. **한 판을 도는 동안 아홉 군데를 지나간다.**

- 서버가 어떤 조각을 어디 깔았는지 `WELCOME` 의 `sector_kind[9]` 로 알려준다.
  씨앗에서 클라이언트가 다시 뽑게 하면 섞는 방식이 양쪽에 적히고, 한쪽만 고치면 갈린다
- **물과 하늘과 색보정은 판 하나에 하나다.** 그래야 아홉 군데가 서로 다른 데면서도
  같은 판 안에 있는 것으로 보인다. 이쪽은 씨앗으로 고른다 (한낮 · 저녁 · 흐림 · 새벽)

**그림은 파일이 하나도 없다.** 바닥도 벽도 캐릭터도 전부 캔버스에 그린다 (13절).

### 2.10 사운드 디자인

**8/31 에는 효과음도 만들어 냈고, 9/1 에 녹음물로 바꿨다.**
파일이 없다는 장점이 있었는데 돌려보니 그게 다였다.
사인파를 아무리 층으로 쌓아도 **물이 터지는 소리로는 안 들린다.**
사람이 실제로 뭔가를 때려서 녹음한 것과는 애초에 다른 물건이다.

Kenney (kenney.nl) 의 **CC0** 팩을 쓴다. 한 사람이 만든 것이라 음색이 안 튄다.
출처는 README 에 적어뒀다 (13절).

**녹음물을 쓴다고 저절로 좋아지지 않는다.** 파일 하나를 그대로 트는 것이 제일 흔한 실수다.

| 문제 | 대응 |
|---|---|
| 열 번째부터 기계로 들린다 | 낼 때마다 **음높이를 흔든다.** 상자 부수는 소리는 백 번을 들어도 다르다 |
| 얄팍하다 | 한 사건에 **파일 두세 개를 겹친다.** 마무리는 유리 + 주먹 + 젖은 몸통 세 장 |
| 공간이 없다 | 화면에서 난 자리대로 **좌우로 벌린다.** 울림을 지나가게 한다 |
| 겹치면 찢어진다 | 마지막에 압축기를 하나 둔다 |
| 음악과 싸운다 | 큰 소리가 나면 음악이 물러난다 (덕킹) |

**어떤 파일을 언제 쓰는지는 `web/audio.js` 맨 위 `CLIPS` 표 한곳에 모여 있다.**
들어보고 마음에 안 들면 파일 이름만 바꾸면 된다. 코드는 안 건드린다.

- **울림(리버브)은 파일이 아니다.** 잡음 한 덩어리를 만들어 뒤로 갈수록 깎으면 그게 방이 된다
- **덕킹** — 폭발이 나면 음악이 잠깐 물러난다. 없으면 둘이 같은 자리에서 싸워 둘 다 안 들린다
- **압축기** — 폭발이 여러 개 겹칠 때 큰 것만 눌러준다. 없으면 조용할 땐 안 들리고 시끄러울 땐 찢어진다

**음악은 녹음한 것이 아니라 판 상황에 따라 층이 붙었다 빠진다.**

| 층 | 언제 |
|---|---|
| 낮은 맥박 | 판이 도는 내내 |
| 리듬 | 사람이 줄기 시작하면 (75% 아래) |
| 아르페지오 | 절반 아래로 줄면 |
| 높은 음 | 마지막 구역, 물이 차오를 때 |

**남은 사람 수를 화면에서 안 읽어도 귀로 알게 하는 것**이 목적이다.
음은 마이너 펜타토닉 다섯 개만 쓴다. 아무 순서로 쳐도 안 어긋나서 리듬만 짜면 된다.

박자는 `setInterval` 로 치지 않는다. 그건 몇십 ms 씩 흔들린다.
**오디오 시계에 0.25초 앞을 미리 예약한다.** 프레임이 밀려도 소리는 제자리에 떨어진다.

### 2.11 HUD

판 위에 글자를 안 쓰기로 했으므로 읽을 것이 전부 여기 모인다.
**브라우저 기본 글꼴로 왼쪽 위에 늘어놓으면 그건 개발자 도구지 게임이 아니다.**
그래서 캔버스가 직접 그린다. 판때기를 깔고, 크기로 위아래를 나누고,
숫자는 크게 이름표는 작고 넓게 쓴다. 흔들림은 HUD에 안 걸린다.

| 자리 | 무엇 | 왜 |
|---|---|---|
| 위 왼쪽 | 판 번호와 단계 | |
| 위 가운데 | **생존 수 + 슬롯 24칸** | 숫자만 있으면 몇인지는 알아도 **누가** 남았는지 모른다. 내 칸만 하얗다 |
| 위 오른쪽 | 경과 시간 | 침수 일정이 몇 분에 오는지가 이 숫자로만 읽힌다 |
| 아래 왼쪽 | 내 능력치 3종을 **칸으로** | 숫자만 적으면 상한까지 얼마 남았는지를 모른다 |
| 아래 오른쪽 | 미니맵 3×3 | 9/3에 AOI 를 붙이면 화면이 한 구역으로 좁아진다. 그때 도망칠 방향을 여기서 정한다 |
| 오른쪽 위 | 킬 피드 | 색 점 + `P3 ▸ P5` |

## 3. 카메라와 시야 — 9/1 에 붙였다

**화면은 내 구역(15×13) + 가장자리 밖 세 칸 = 21×19 칸이다.**

AOI 가 내 구역 사람만 보내주는데 화면이 판 전체를 보여주면 옆 구역 사람이 그냥 사라져서
버그로 보인다. **보내는 것과 보이는 것을 같게** 만드는 것이 이 절의 요지다.

- 바깥 세 칸에는 **안개**를 씌운다. 지형과 물은 보이고 사람은 안 보인다.
  서버가 거기 사람을 안 보내주므로 **안 보이는 게 맞다.**
  안개를 씌우면 그게 규칙이 아니라 날씨처럼 보인다
- **발을 들인 순간 바로 바꾼다.** 처음에는 경계를 두 칸 넘어야 바뀌게 했는데(히스테리시스),
  그러면 이미 새 구역에 서 있는데 옛 구역을 보고 있게 된다. 그 사이에 안 보이는 사람에게 맞는다.
  떨림은 다른 방법으로 막는다 — **목표만 즉시 바꾸고 화면은 미끄러지듯 따라간다.**
  남은 거리의 일정 비율씩 좁히므로 처음엔 빠르고 도착할수록 느려진다
- 죽으면 아직 살아 있는 사람을 따라간다. 빈 구역을 보고 있으면 관전이 아니라 정지 화면이다

**덤으로 얻은 것.** 판이 45×39 에서 21×19 로 좁아진 만큼 타일이 커졌다.
같은 창에서 **19px → 39px.** 캐릭터 얼굴과 표정이 그제서야 보인다.



- 카메라는 **구역 단위로 전환**. 스크롤하지 않음
- 캐릭터가 속한 구역 15×13을 화면에 꽉 채움
- **경계를 2칸 이상 넘어야 전환** (히스테리시스 — 안 넣으면 화면이 떨린다)
- **회색 주변부 3칸** — 인접 구역 일부를 흐리게 표시

회색 주변부에 무엇을 보여줄 것인가가 곧 4절의 AOI 정책이다.

---

## 4. AOI — 엔티티 타입별 전파 정책

AOI 셀 = 맵 조각 = 15×13. 카메라 단위와 일치.

| 엔티티 | 전파 범위 | 빈도 | 이유 |
|---|---|---|---|
| 플레이어 위치 | 현재 구역만 | 매 틱 | 연속 좌표라 데이터가 크고 잦음. **AOI의 주 대상** |
| 물풍선 · 물줄기 | 현재 + 인접 3칸 | 이벤트 | **위험은 보여야 함.** 넘어가자마자 죽으면 억울 |
| 지형 파괴 | 현재 + 인접 3칸 | 이벤트 | 회색 주변부 지형과 일치시켜야 |
| 아이템 | 현재 구역만 | 이벤트 | 파밍 정보 우위를 안 주기 위해 |
| 침수 상태 | **전역** | 초당 1회 | 10바이트 미만. 거르는 게 오히려 낭비 |
| 킬 피드 · 생존자 수 | **전역** | 드물게 | 동일 |
| 킬 표식(3킬 이상) | **전역** | 드물게 | 강자를 견제 대상으로 만들어 1등 독주 방지 |

**교훈으로 정리할 것:** AOI는 대역폭 최적화 수단이지 게임 규칙이 아니다.
작고 드물게 바뀌는 전역 상태는 그냥 전원에게 보내는 게 맞다.
크고 자주 바뀌는 것만 거른다.

**AOI 예외** — 유령(가까워도 안 보냄), 레이더(멀어도 보냄).
거리 필터가 아니라 게임 규칙이 개입하는 계층으로 설계.

---

## 5. 관전 모드

- 사망 시 자동 전환
- **A / D 키로 생존자 간 전환**. 선택한 사람의 구역 카메라를 그대로 따라감
- 구현: 이미 만든 AOI를 관전 대상 기준으로 재사용
- **주의 — 측정에서 반직관적 결과가 나올 것**: 후반에 생존자는 5명인데
  관전자가 19명이라 트래픽이 안 줄어든다. 이 그래프가 좋은 설명거리

---

## 6. 프로토콜

고정 헤더 + 페이로드. 리틀 엔디안(서버·클라 모두 같은 PC 전제, **명시적 결정**).

```
[ uint16 size ][ uint16 packetId ][ payload ... ]
```

| 방향 | 이름 | 페이로드 |
|---|---|---|
| C→S | ENTER | nickname |
| C→S | INPUT | dir(u8), seq(u32) |
| C→S | PLACE_BUBBLE | seq(u32) |
| C→S | USE_ITEM | seq(u32) |
| S→C | ENTER_ACK | myId, mapW, mapH, tickRate, sectorLayout |
| S→C | SPAWN / DESPAWN | id[] (AOI 진입·이탈) |
| S→C | STATE | tick, (id, x, y, judgeTile)[] |
| S→C | BUBBLE_PLACED / EXPLODED | |
| S→C | BLOCK_DESTROYED / ITEM_SPAWNED / ITEM_TAKEN | |
| S→C | TRAPPED / ESCAPED / DIED | |
| S→C | FLOOD_WARN / FLOOD_CLOSE | 전역 |
| S→C | KILL_FEED / ALIVE_COUNT | 전역 |

`seq`는 클라이언트 예측·보정용. 서버는 받은 seq를 그대로 되돌려준다.

---

## 7. 서버 구조

- IOCP + 워커 스레드 풀
- 세션 단위 수신 버퍼 — 커서 두 개(read/write)를 둔 연속 버퍼.
  자리가 모자랄 때만 앞으로 당긴다. 링으로 안 한 이유는 헤더가 이음매에 걸치면
  패킷 경계를 잘라내는 일이 복잡해지기 때문이다. 송신은 순서대로 흘려보내기만
  하면 되므로 그쪽을 링으로 둔다
- 게임 시뮬레이션은 **단일 틱 스레드**가 소유 (락 최소화)
- 워커 → 틱 스레드로는 Job Queue를 통해 전달
- **모든 공유 자료구조에 소유 스레드를 주석으로 명시**
- 클라이언트를 신뢰하지 않음 — 이동은 입력만 받고 서버가 속도 상한을 검증

---

## 8. 상수 (전부 한 파일에)

`GameConstants.h` 하나에 몰아둔다. 봇 시뮬레이션은 이 파일만 고치며 돌린다.

```
TICK_RATE            = 30
TILE_UNITS           = 256     // 고정소수점 분할
PLAYER_BODY_NUM/DEN  = 80/100  // 몸 크기. 두 칸에 걸쳐 설 수 있게 하는 값
MOVE_SPEED_BASE      = 18      // 2.1 타일/초. 롤러 상한까지 먹으면 5.4
CORNER_ASSIST        = 0       // 껐다. 벽에 닿으면 선다
LANE_SNAP_PERCENT    = 50      // 좁은 데로 들어갈 때만 내 칸 한가운데로 당긴다
TRAP_MOVE_SPEED      = 6       // 갇혔을 때의 속도. 기본의 1/4
SECTOR_W, SECTOR_H   = 15, 13
SECTOR_COUNT         = 3 x 3
PLAYER_MAX           = 24
PEEK_TILES           = 3       // 회색 주변부
CAMERA_HYSTERESIS    = 2
BUBBLE_FUSE_TICKS    = 75      // 2.5초
TRAP_DURATION_TICKS  = 210     // 7초
FLOOD_ESCAPE_TICKS   = 60      // 2초
BLAST_BASE_RANGE     = 1       // 시작 사거리. 사방 한 칸
BLOCK_FILL_PERCENT   = 100     // 고정 벽과 스폰 빼고 전부 상자
BOX_PERCENT          = 20      // 그중 다섯에 하나는 밀 수 있는 상자
PUSH_COOLDOWN_TICKS  = 10      // 상자를 민 뒤 쉬는 시간
BOT_FILL_TO          = 12      // 빈자리를 봇으로 채운다
STAT_CAP_FROM_WALL   = 4
STAT_CAP_SPEED       = 7       // 롤러만 상한이 다르다
KILL_DROP_RATIO      = 0.5
ITEM_DROP_CHANCE     = 0.40
```

---

## 9. 측정 (9/3)

### 9.1 AOI 최적화 — 최우선

**어떻게 쟀나.** `Server.exe aoi 0` / `aoi 1` 로 같은 서버를 두 번 돌리고,
`walker.exe 12 30` 으로 사람 12명이 30초간 돌아다니게 했다. 봇 12명이 같이 판을 돈다.
서버가 매초 `[Net]` 한 줄을 찍는다. **스위치 하나로 전후를 재게 만들어둔 것이 핵심이다.**

| 지표 | 전 (전원 브로드캐스트) | 후 (AOI) | 변화 |
|---|---|---|---|
| 초당 송신 바이트 (평균) | 116,400 | **29,316** | **−75%** |
| 초당 송신 바이트 (최대) | 145,356 | 50,008 | −66% |
| 초당 송신 패킷 (평균) | 755 | 443 | −41% |
| 초당 송신 패킷 (최대) | 2,076 | **673** | **−68%** |
| 스냅샷 생성 (초당) | 30 | 242 | **8.1배** |
| 버린 주문 | 0 | 0 | |

**최대치가 더 크게 줄었다.** 폭발이 터지는 순간이 제일 위험한 구간인데,
그때 나가는 이벤트가 구역 밖 사람에게는 안 가기 때문이다.
평균보다 **최대**가 중요하다. 송신 버퍼가 터지는 건 평균이 아니라 최대에서다.

**AOI 는 공짜가 아니다.** 대역폭을 CPU 로 바꾼 것이다.
스냅샷을 초당 30번 만들던 것을 242번 만든다. 그래도 8배로 끝난 이유는
**사람마다가 아니라 구역마다** 만들어 돌려 쓰기 때문이다.
사람마다 만들었으면 24배가 됐다. AOI 셀을 화면과 같게 잡은 덕이다.

**시간대별.** 초반에는 아홉 구역에 흩어져 있어서 생성 횟수가 많고(구역당 한 번)
아끼는 양도 크다. 후반에는 다들 한 구역에 몰려서 생성은 한두 번으로 줄지만
아끼는 양도 같이 줄어든다. **AOI 는 흩어져 있을 때 일하고 몰리면 논다.**
그런데 몰릴 때는 사람 수 자체가 적어서 그래도 된다.

### 9.1.1 AOI 를 붙이다 잡은 진짜 버그

부하 시험에서 **접속 757 / 정리 536** 이 나왔다. 221개가 유령으로 남았다.

원인은 AOI 가 아니라 원래 있던 것이었다. 주문 꽂이가 차면 `PushJob` 이 false 를
돌려주는데, **입·퇴장은 그 반환값을 아무도 안 봤다.** 퇴장이 버려지면
나간 사람이 판에 남고, 그 자리는 영영 안 비고, 세션 참조도 안 내려간다.

AOI 가 서버를 빠르게 만들어서 부하 시험이 더 많은 접속을 밀어 넣었고,
그래서 원래 있던 이 구멍이 드러났다.

**고친 방식은 통을 크게 만드는 게 아니라 나누는 것이다.**

| 주문 | 버려도 되나 | 왜 |
|---|---|---|
| 패킷 | **된다** | MOVE 를 하나 놓치면 한 틱 더 같은 방향으로 갈 뿐이다 |
| 입·퇴장 | **안 된다** | 놓치면 나간 사람이 유령으로 남는다 |

통을 나누고 틱 스레드가 **입·퇴장부터** 처리한다.
입장이 그 사람 패킷보다 먼저여야 하기 때문이다.
퇴장이 남은 패킷보다 먼저 처리되는 건 괜찮다. 자리가 이미 비어서 그 패킷은 무시된다.

버린 주문 수를 매초 로그에 찍는다. **0 이 아니면 그건 사고다.**

### 9.2 밸런스 검증 (봇 시뮬레이션)

공격적 봇 12 vs 소극적 봇 12, 100판. **생존 순위 분포**를 집계.
소극적 봇이 상위권을 휩쓸면 밸런스가 잘못된 것 → 상수 수정 후 재실행.

> 플레이테스터가 없는 상황에서 밸런스를 감으로 정할 수 없어,
> 성향이 다른 봇으로 시뮬레이션해 적극적 플레이가 실제로 보상받는지 검증했다.

### 9.3 지연이 게임플레이에 미치는 영향

더미 클라이언트에 인위적 지연을 삽입하고 두 축을 측정한다.
**걸치기 = 이동의 정밀도**, **바늘 탈출 = 타이밍의 정밀도.**

| 핑 | 걸치기 성공률 (예측 OFF / ON) | 바늘 탈출 성공률 |
|---|---|---|
| 20ms | | |
| 50ms | | |
| 100ms | | |
| 200ms | | |

바늘은 연쇄가 도달하기 직전 한 틱 차이로 생사가 갈리므로 지연에 특히 민감하다.

### 9.4 틱레이트

20 / 30 / 60Hz에서 걸치기 성공률과 트래픽. "30Hz면 충분한가"에 근거를 만든다.

측정 조건(맵, 봇 행동, 측정 시간, 머신 사양)을 README에 함께 적는다.
조건 없는 숫자는 면접에서 반드시 되물린다.

**9.1이 자소서 2번의 중심이다. 9.2~9.4는 시간이 남을 때 붙인다.**
9.1 없이 9.3을 먼저 하면 순서가 거꾸로다.

---

## 10. 구현 우선순위

**반드시 (9/1~9/2)**
고정소수점 이동 + 걸치기 · 물풍선 설치/퓨즈/십자/연쇄 · 갇힘 7초 ·
블록 파괴 + 확률 드롭 + 수치 3종 · 침수 4단계 · AOI · 웹 클라이언트

**여유 보고 (9/4) — 9/2 에 정리했다**

넣었다: 킬 드롭 · 킬 피드 · 생존자 수 · 관전 모드 · 맵 조각 10종 ·
최종 보급 · 효과음 · 봇 · 결과 화면 · 미는 상자

**자른다: 특수 아이템 5종 · 킬 표식.** 2.5 절에 왜 잘랐는지 적어뒀다.

**문서로만**
리플레이 · 재접속 · 지연 실험 확장 · 매치메이킹

**남은 닷새에 안 하는 것 (9/2 결정)**

| 안 하는 것 | 왜 |
|---|---|
| 특수 아이템 5종 | 규칙이 다섯 개 늘고 봇이 못 써서 밸런스에 안 잡힌다 |
| 맵 조각 추가 | 열 개면 아홉 자리를 채운다. 열한 번째는 아무것도 증명 안 한다 |
| AOI 자료구조 재작성 | 재보고 병목이면 그때. 방금 그 자리에서 자물쇠 버그가 났다 |
| 화면 다시 만들기 | 9/1 에 다시 만들었다. 또 만지면 하루가 없어진다 |
| 소리 추가 | 안 들리는 건 없어서가 아니라 안 섞여서다 |

**아예 안 한다**
DB · 계정/인증 · 매칭메이킹 구현 · 암호화 · 프로토콜 버전 관리 · 엔진 연동 · 예쁜 UI.
전부 이 9일에 넣으면 아무것도 안 끝난다.
면접에서 물으면 "못 한 것"이 아니라 **"자른 것"**으로 답한다.

> 포트폴리오에는 이렇게 쓴다 — "설계는 여기까지, 9일 안에 구현한 건 여기까지다.
> 남은 것은 이 순서로 붙일 계획이고, 그렇게 자를 수 있는 구조로 만들어뒀다."
> 다 만든 척하는 것보다 범위를 스스로 자를 줄 아는 게 실무자로 보인다.

---

## 11. 일정

각 날의 **완료 조건**을 통과해야 다음 날로 넘어간다.
통과 못 하면 그날 밤에 다음 날 범위를 줄인다. **밀린 채로 진행하지 않는다.**

### 8/27 (목) — 세팅 · blocking 에코 ✅ 완료

- VS2022 솔루션, `.gitignore` 선(先) 커밋, Public 레포
- blocking TCP 에코 서버 + 콘솔 클라이언트
- **완료 조건:** 클라이언트에서 친 문자열이 되돌아온다 → **통과**

### 8/28 (금) — IOCP 코어 · 4단계 학습 루프

- 오전: `select` 기반 다중 접속으로 논블로킹 개념 체득
- 오후: IOCP 전환 — 완료 포트 생성, 워커 1개, `WSARecv` 1회전
- **완료 조건:** IOCP로 에코가 동작한다.
  `OVERLAPPED`가 왜 필요한지 코드 없이 말로 설명할 수 있다

### 8/29 (토) — 세션 생명주기 · 4단계 학습 루프

- `Session`(소켓 + 버퍼 + 상태), `SessionManager`
- `OVERLAPPED` 확장 구조체로 Recv/Send 구분
- 강제 종료 · half-close · 참조 카운트
- **완료 조건:** 클라 10개를 붙였다 랜덤하게 끊어도 서버가 죽지 않는다

### 8/30 (일) — 링버퍼 · 패킷 경계 · 4단계 학습 루프

- 수신 버퍼 직접 구현 (커서 두 개 + 필요할 때만 당기기)
- 고정 헤더 파싱, **분할 수신 / 연접 수신** 처리
- 송신 큐(링)는 브로드캐스트를 붙일 때 필요해진다. 그때 만든다
- **완료 조건:** 1바이트씩 쪼개 보내도, 여러 패킷을 한 번에 몰아 보내도 안 깨진다
- ※ 이 날의 버그가 자소서 2번 소재 1순위다. 로그를 특히 자세히

### 8/31 (월) — 워커 스레드 · 락 설계 · 4단계 학습 루프 → 8/30 에 앞당겨 완료

- 워커 스레드 풀(N개), Job Queue
- 락 설계 — 세션별인가 룸별인가, 무엇을 누가 소유하는가
- **완료 조건:** 스레드 4개에서 동시 접속·이탈·브로드캐스트가 레이스 없이 돈다.
  각 자료구조의 소유 스레드를 주석 없이 답할 수 있다

> **8/31까지 A구역 네 개를 끝내는 것이 유일한 필수 조건이다.**
> 여기가 밀리면 9/2 체크포인트에서 게임 로직을 버려야 한다.

### 8/31 (월) — 틱 스레드 + Job Queue · A구역, 4단계 학습 루프  ✅

8/30 에 A구역 네 개를 다 끝내서 하루가 비었다. 게임 로직 앞의 마지막 구조 작업을 여기서 한다.

지금은 워커 넷이 각자 패킷을 처리한다. 게임은 그러면 안 된다.
게임 상태를 여러 스레드가 만지면 락이 걷잡을 수 없이 늘어난다.

```
지금   워커1..4 -> 각자 처리
필요   워커1..4 -> [Job Queue] -> 틱 스레드 하나가 30Hz 로 꺼내 처리
                                  게임 상태는 이 스레드만 소유한다
```

- Job Queue (워커가 넣고 틱 스레드가 꺼낸다. 여기도 공유물이라 자물쇠가 붙는다)
- 30Hz 틱 루프
- 틱 스레드에서 브로드캐스트
- **완료 조건:** 패킷이 워커에서 틱 스레드로 넘어가 처리되고, 게임 상태를
  틱 스레드만 만진다는 것을 말로 설명할 수 있다

이건 "워커 스레드 구조 · 락 설계"라 CLAUDE.md A구역이다. 4단계 루프를 돈다.
여기까지 되면 9/1 은 게임 규칙만 채우면 된다.

### 9/1 (화) — 게임 로직 코어 · AI 가 그냥 만든다

CLAUDE.md "2026-08-31 변경" 절대로 간다. 학습 루프를 안 돌린다.
**걸치기 판정만 구술 테스트를 남긴다.** SPEC 1절 첫 줄이라 여기서 막히면 안 된다.

- 고정소수점 이동 + 걸치기, 물풍선 설치/퓨즈/십자/연쇄, 갇힘 7초
- 블록 파괴 + 확률 드롭 + 수치 아이템 3종
- 30Hz 틱 루프
- **완료 조건:** 클라이언트 없이 로그만으로 시뮬레이션이 정상 동작한다.
  걸치기가 `PLAYER_BODY_NUM/DEN` 대로 판정된다

> 8/31에 이동·걸치기·맵까지 앞당겨 끝냈다. `tools/movetest.exe` 가 15 PASS 로 증명한다.

### 9/2 (수) — 침수 + 웹 클라 + 2인 플레이 · ★ 체크포인트

- 침수 4단계, Node WebSocket 브리지, 웹 Canvas 클라이언트
- **완료 조건:** 두 명이 웹에서 붙어 물풍선으로 서로를 잡을 수 있다
- **판정:** 이날 밤까지 위가 안 되면 게임 로직을 버리고 채팅·룸 서버로 축소한다.
  그리고 9/3부터 서류로 전환한다. 미련 없이 자른다

### 9/3 (목) — ★ AOI + 1차 측정

- 더미 클라이언트 부하 테스트 도구 (C구역 — AI에 위임)
- **1차 측정:** 봇 24, 전원 브로드캐스트 → 초당 패킷 / 대역폭 / CPU / 평균 지연
- **AOI 구현:** 셀 분할, 인접 전파, 엔티티 타입별 정책, SPAWN/DESPAWN
- **2차 측정** → 전후 비교표
- **완료 조건:** 9.1 전후 수치가 표로 나온다.
  이 표가 자소서 2번과 포트폴리오의 중심이다

### 9/4 (금) — 여유 기능 + 나머지 측정

- 10절 "여유 보고" 목록에서 되는 것만. **안 되면 그냥 버린다**
- 9.2 밸런스 봇 시뮬 / 9.3 지연 / 9.4 틱레이트 중 되는 것
- **완료 조건:** 없다. 이 날은 버퍼다. 9/3이 밀렸으면 여기서 흡수한다

### 9/5 (토) — ★ 기능 동결

- 오전에 동결. 이후 새 기능 금지, 정리만
- 시연 영상 — 봇 24 + 침수 + AOI 시각화
- 코드 정리, 구조도, README, 9일치 개발 로그 정리
- 저녁에 자소서 4문항 초안
- **완료 조건:** 처음 보는 사람이 README만 읽고 빌드해서 돌릴 수 있다

### 9/6 (일) — 서류 · 제출

자소서 마무리, 프로젝트 이력, 자유설문, 포트폴리오 PDF, 전체 검수, **제출**

> 서류가 하루로 줄었다. 자소서의 재료는 `devlog/` 에서 나오므로
> **9/1부터 매일 devlog 를 쓸 때 자소서에 그대로 넣을 문장을 한두 개씩 골라둔다.**
> 9/6에 백지에서 시작하면 안 된다.

### 9/7 (월) — 예비일

16:00 전 제출 상태 확인만

---

## 12. 매일 남길 개발 로그

`devlog/YYYY-MM-DD.md`, 세 줄.

```
1. 오늘 하려던 것:
2. AI에게 물은 것 / 어떻게 물었나:
3. AI가 틀린 것 / 내가 어떻게 잡았나:
```

**8/31부터 2번의 성격이 바뀐다.** 서버가 끝나고 게임 쪽은 AI가 그냥 만들기로 했으므로
(CLAUDE.md "2026-08-31 변경" 절), 2번은 **"뭘 시켰고 어떻게 잘라서 시켰나"**를 적는다.
한 번에 다 시켰는지, 쪼개서 시켰는지, 무엇을 못 하게 막았는지.
AI가 더 많이 쓸수록 **3번이 유일한 검증 증거**가 되므로 3번은 그대로다.

3번이 비는 날은 A구역을 안 건드린 날이다.
이 파일들이 10/3 AI 활용 역량평가의 재료이고, 포트폴리오 마지막 섹션이며,
공고 필수요건 ③("생성형 AI 도구를 활용해 코드를 작성·**검증**할 수 있는 분")의 증거물이다.

---

## 13. 외부 리소스

효과음 등 외부 에셋을 쓰면 **출처와 라이선스를 README에 기록한다.**
공고에 명시된 유의사항이며, 이 레포는 Public이다.
