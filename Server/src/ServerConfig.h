// Server/src/ServerConfig.h — 서버만 쓰는 설정
//
// 게임 규칙 상수는 Common/GameConstants.h 에 있다. 여기는 서버 운영 값만 둔다.
#pragma once

constexpr int WORKER_COUNT = 4;    // 워커 스레드 수. 코어 수 정도면 된다
constexpr int MAX_SESSION  = 256;  // 동시 접속 상한. 세션 목록 배열 크기다
constexpr int MAX_JOB = 1024;  // 한 틱에 담을 수 있는 패킷 주문 수. 넘치면 틱이 밀렸다는 뜻이다

// 한 틱에 담을 수 있는 입·퇴장 주문 수.
//
// 패킷과 따로 둔다. 패킷은 버려도 되지만 입·퇴장은 버리면 사람이 유령으로 남는다.
// 한 틱(33ms)에 256명이 들락날락하는 일은 없다. 그래도 작게라도 따로 있어야
// 패킷이 몰아칠 때 같이 밀려나지 않는다
constexpr int MAX_LIFE = 1024;

// 처음에 256 으로 뒀다가 부하 시험에서 53개가 버려졌다.
// 초당 75명이 붙었다 끊는 상황이라 한 틱에 몰릴 때가 있다.
// 하나에 16바이트라 1024 로 올려도 통 두 개가 32KB 다. 안 아깝다.
// **버린 입·퇴장 수는 0 이어야 한다.** 0 이 아니면 그건 사고고, 로그에 매초 찍힌다
