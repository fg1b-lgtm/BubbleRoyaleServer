// 연습 — blocking TCP 에코 클라이언트 (원본: Client/src/main.cpp)
//
// 여기는 3단계 "지우고 다시 쓰기" 자리다.
//
// 1. 원본에 달아둔 '내 말로 된 한글 주석'만 여기 옮겨 적는다. 코드는 옮기지 않는다.
// 2. 그 주석만 보고 처음부터 직접 친다. 원본은 열지 않는다.
// 3. build.bat d11-echo\client.cpp 로 컴파일한다.
// 4. 에러가 난 줄만 원본을 본다. 전체를 다시 보지 않는다.
//
// 서버와 다른 점을 스스로 짚어보면서 써라 —
//   bind / listen / accept 가 없는 이유는?
//   상자가 왜 두 개인가?
//   순서가 왜 send → recv 인가? (서버는 반대다)
//
// 시도 기록:
//   1차 (  월  일) — 막힌 곳:
//   2차 (  월  일) — 막힌 곳:

// ↓ 여기부터 직접 쓴다
